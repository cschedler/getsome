<h1>New Movements</h1>

<% if @movement.errors.any? %>

    <div id="error_explanation">

      <h2><%= pluralize(@movement.errors.count, "error") %> prohibited
        this post from being saved:</h2>
      <ul>
        <% @movement.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>

  <% end %>

<%= form_with scope: :movement, url: movements_path, local: true do |t| %>
	<p>
		<%= form.label :movement %>
		<%= form.label :set %>
		<%= form.label :rep %>
		<%= form.label :weight %>
		<%= form.label :duration %>
	</p>

	<p>
		<%= form.text_field :movement %>
		<%= form.text_field :set %>
		<%= form.text_field :rep %>
		<%= form.text_field :weight %>
		<%= form.text_field :duration %>
	</p>
<% end %>
