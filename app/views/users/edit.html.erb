<h1>Edit User</h1>

<%= form_with(model: @user) do | form| %>
	<p>
		<%= form.label :name %>
		<%= form.text_field :name %>
	</p>
	<p>
		<%= form.label :email %>
		<%= form.text_field :email %>
	</p>
	<p>
		<%= form.submit %>
	</p>
<% end %>

<%= link_to'Back', user_path(@user) %>
